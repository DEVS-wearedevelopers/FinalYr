import InstitutionInbox from '@/screens/Institution/InstitutionInbox';
export const metadata = { title: 'Inbox — Institution | DOMRS', description: 'System notices and PHO advisories for your facility.' };
export default function Page() { return <InstitutionInbox />; }
