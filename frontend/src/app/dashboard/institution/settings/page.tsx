import InstitutionSettings from '@/screens/Institution/InstitutionSettings';
export const metadata = { title: 'Settings — Institution | DOMRS', description: 'Manage your institution profile and editable settings.' };
export default function Page() { return <InstitutionSettings />; }
